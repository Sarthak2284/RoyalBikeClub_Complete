<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Thank You for Renting</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f0f4f8;
            font-family: 'Arial', sans-serif;
        }
        .card {
            background-color: #ffffff;
            border: none;
            border-radius: 1rem;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
        }
        .card-header {
            background-color: #004e92;
            color: #ffffff;
            border-bottom: none;
            border-top-left-radius: 1rem;
            border-top-right-radius: 1rem;
        }
        .card-title {
            font-size: 1.75rem;
        }
        .btn-primary {
            background-color: #0069d9;
            border-color: #0062cc;
            transition: background-color 0.3s ease, box-shadow 0.3s ease;
        }
        .btn-primary:hover {
            background-color: #0056b3;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
        }
        .card-summary {
            background-color: #f8f9fa;
            border: 1px solid #dee2e6;
            border-radius: 0.5rem;
        }
        .card-summary .card-body {
            padding: 1.5rem;
            color: #333;
        }
        .card-text {
            color: #333;
        }
    </style>
</head>
<body>
    <div class="container mt-5">
        <div class="card mx-auto shadow-sm" style="max-width: 700px;">
            <div class="card-header text-center">
                <h1 class="card-title">Thank You for Renting, <%= userName %>!</h1>
            </div>
            <div class="card-body text-center">
                <p class="card-text mb-4">Your rental of <strong><%= bike.name %></strong> has been successfully completed.</p>
                
                <!-- Rental Summary -->
                <div class="card card-summary mb-4 mx-auto" style="max-width: 80%;">
                    <div class="card-body">
                        <h5 class="card-title">Rental Summary</h5>
                        <p class="card-text"><strong>Bike Model:</strong> <%= bike.name %></p>
                        <p class="card-text"><strong>Number of Days:</strong> <%= numberOfDays %></p>
                        <p class="card-text"><strong>Total Payment:</strong> ₹<%= totalPayment %></p>
                    </div>
                </div>

                <a href="/" class="btn btn-primary">Go to Home</a>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
