<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Events - Royal Bike Club</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-purple-900 min-h-screen">
    <div class="container mx-auto px-4 py-12">
        <!-- Go Back Button at Top Left Corner -->
        <div class="mb-4">
            <a href="javascript:history.back()" class="bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded-lg">
                Go Back
            </a>
        </div>
        
        <h1 class="text-4xl font-bold text-white text-center mb-8">Upcoming Events</h1>
        
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            <% events.forEach(event => { %>
                <div class="bg-white rounded-lg shadow-lg overflow-hidden">
                    <img src="<%= event.image %>" alt="<%= event.name %>" class="w-full h-48 object-cover">
                    <div class="p-6">
                        <h2 class="text-2xl font-bold mb-2"><%= event.name %></h2>
                        <p class="text-gray-600 mb-4"><%= event.description %></p>
                        <p class="text-blue-500 font-semibold mb-2">Date: <%= event.date.toDateString() %></p>
                        <a href="/register/<%= event.slug %>" class="bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded-lg">
                            Register Now
                        </a>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
</body>
</html>
