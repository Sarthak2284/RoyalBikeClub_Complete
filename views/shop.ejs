<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shop - Royal Bike Club</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
            font-family: Arial, sans-serif;
        }

        .header {
            background-color: #343a40; /* Dark background for header */
            color: white;
            padding: 30px 0;
        }

        .header h1 {
            font-size: 2.5rem; /* Increased font size for heading */
            margin: 0;
        }

        .card {
            position: relative;
            overflow: hidden;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            margin-bottom: 30px;
            border: none;
            border-radius: 15px; /* Rounded corners for cards */
        }

        .card img {
            max-height: 200px;
            object-fit: cover;
            transition: transform 0.3s ease; /* Smooth zoom on hover */
        }

        .card-body {
            display: flex;
            flex-direction: column;
            justify-content: center;
            padding: 15px;
            text-align: center; /* Center text in card body */
        }

        .card:hover {
            transform: scale(1.05);
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
        }

        .card-info {
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            background: rgba(0, 0, 0, 0.7);
            color: white;
            padding: 20px;
            opacity: 0;
            visibility: hidden;
            transition: opacity 0.3s ease, visibility 0.3s ease;
        }

        .card:hover .card-info {
            opacity: 1;
            visibility: visible;
        }

        .card-info h5,
        .card-info p {
            margin: 0;
        }

        .card-info .btn {
            margin-top: 10px;
            background-color: #007bff; /* Custom button color */
            border: none;
        }

        .card-info .btn:hover {
            background-color: #0056b3; /* Darker button color on hover */
        }

        .go-back-btn {
            color: white;
            text-decoration: none;
            height: 40px;
            width: 90px;
            display: flex;
        }

        .shop{
            text-align: justify;
        }
        @media (max-width: 768px) {
            .header {
                padding: 20px;
            }

            .header h1 {
                font-size: 2rem; /* Decrease font size on mobile */
            }

            .card-body {
                padding: 10px;
            }

            .card-info {
                padding: 10px;
            }
        }
    </style>
</head>

<body>
    <header class="header text-center">
        <div class="container">
            <div class="d-flex align-items-center justify-content-between">
                <a href="/welcome" class="btn btn-outline-light me-2 go-back-btn">
                   Go Back
                </a>
                <h1 class="text-3xl font-bold text-white flex-grow text-center shop">Shop</h1>
            </div>
        </div>
    </header>
    <div class="container mt-5">
        <div class="row">
            <% bikes.forEach(bike => { %>
                <div class="col-md-4">
                    <div class="card">
                        <img src="<%= bike.image %>" class="card-img-top" alt="<%= bike.name %>">
                        <div class="card-info">
                            <h5 class="card-title"><%= bike.name %></h5>
                            <p class="card-text">Price: ₹<%= bike.price %></p>
                            <p class="card-text">Category: <%= bike.category %></p>
                            <p class="card-text"><%= bike.description %></p>
                            <a href="/buy-now/<%= bike._id %>" class="btn btn-primary">Proceed to Pay</a>
                        </div>
                        <div class="card-body d-flex justify-content-center align-items-center">
                            <h5 class="card-title d-none d-md-block"><%= bike.name %></h5>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
