<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Checkout</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #e9ecef;
            color: #495057;
            font-family: Arial, sans-serif;
        }
        .container {
            margin-top: 60px;
        }
        .card {
            border-radius: 0.75rem;
            overflow: hidden;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .card-header {
            background-color: #007bff;
            color: white;
            font-size: 1.5rem;
            font-weight: 700;
        }
        .card-body {
            padding: 2rem;
            background-color: #ffffff;
        }
        .card-title {
            font-size: 1.5rem;
            font-weight: 600;
        }
        .btn-primary {
            background-color: #007bff;
            border-color: #007bff;
            font-size: 1rem;
        }
        .btn-primary:hover {
            background-color: #0056b3;
            border-color: #004085;
        }
        .btn-secondary {
            font-size: 1rem;
        }
        .highlight {
            background-color: #f1f3f5;
            padding: 1.5rem;
            border-radius: 0.5rem;
            border: 1px solid #ced4da;
        }
        .highlight h2 {
            margin-top: 0;
        }
        .btn-wrapper {
            display: flex;
            justify-content: space-between;
            margin-top: 2rem;
        }
        @media (max-width: 576px) {
            .btn-wrapper {
                flex-direction: column;
            }
            .btn-wrapper .btn {
                margin-bottom: 1rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="card">
            <div class="card-header">
                Checkout Summary
            </div>
            <div class="card-body">
                <h1 class="card-title">Welcome, <%= userName %>!</h1>
                <div class="highlight">
                    <h2>Bike Details</h2>
                    <p><strong>Bike Name:</strong> <%= bike.name %></p>
                    <p><strong>Rental Price:</strong> ₹<%= bike.rentalPrice %> per day</p>
                    <p><strong>Number of Days:</strong> <%= numberOfDays %></p>
                    <p><strong>Total Payment:</strong> <%= totalPayment %></p>
                </div>
                <div class="btn-wrapper">
                    <a href="/welcome" class="btn btn-secondary">Go Back</a>
                    <button class="btn btn-primary" onclick="window.print()">Confirm Rental</button>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
