<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bike Meetups - Royal Bike Club</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(to right, #4b006e, #8e44ad);
        }
        .event-card:hover {
            transform: scale(1.05);
            transition: transform 0.3s ease;
        }
    </style>
</head>
<body class="bg-purple-900 min-h-screen flex items-center justify-center">
    <div class="container mx-auto px-4 py-12">
        <h1 class="text-4xl font-bold text-white text-center mb-8">Upcoming Bike Meetups</h1>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            <% events.forEach(event => { %>
                <div class="bg-white rounded-lg shadow-lg overflow-hidden event-card">
                    <img src="<%= event.image %>" alt="<%= event.name %>" class="w-full h-48 object-cover">
                    <div class="p-6">
                        <h2 class="text-2xl font-bold mb-2"><%= event.name %></h2>
                        <p class="text-gray-600 mb-4"><%= event.description %></p>

                        <p class="text-gray-800 font-semibold">
                            <span class="text-blue-500">Location:</span> <%= event.location %>
                        </p>
                        <p class="text-gray-800 font-semibold">
                            <span class="text-blue-500">Date:</span> <%= event.date %>
                        </p>
                        <p class="text-gray-800 font-semibold">
                            <span class="text-blue-500">Entry Fee:</span> ₹<%= event.price %>
                        </p>

                        <div class="mt-4">
                            <a href="<%= event.registrationLink %>" target="_blank" 
                               class="bg-green-500 hover:bg-green-600 text-white px-4 py-2 rounded-lg">
                                Register Now
                            </a>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>

        <div class="mt-8 text-center">
            <a href="/" class="bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded-lg">Back to Home</a>
        </div>
    </div>
</body>
</html>
