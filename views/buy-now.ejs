<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Buy Bike</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <style>
        /* Custom styles for the Go Back button */
        .go-back-btn {
            border: 2px solid #1d4ed8; /* Customize the border color */
            color: #1d4ed8; /* Customize text color */
            transition: background-color 0.3s ease, color 0.3s ease;
        }
        .go-back-btn:hover {
            background-color: #1d4ed8; /* Customize hover background color */
            color: white; /* Change text color on hover */
        }
    </style>
</head>

<body class="bg-gradient-to-r from-gray-100 to-gray-300">
    <div class="container mx-auto p-6">
        <div class="flex items-center justify-between mb-4">
            <a href="/shop" class="go-back-btn py-2 px-4 rounded-lg font-semibold">
                Go Back
            </a>
        </div>
        
        <!-- Main Bike Details -->
        <div class="bg-white p-8 rounded-lg shadow-lg max-w-lg mx-auto transform transition-transform duration-300 hover:scale-105">
            <div class="relative">
                <img src="<%= bike.image %>" alt="<%= bike.name %>" class="w-full h-64 object-cover rounded-t-lg mb-6">
                <div class="absolute inset-0 bg-gradient-to-t from-black via-transparent to-transparent opacity-70 rounded-t-lg"></div>
                <h1 class="absolute bottom-4 left-4 text-3xl text-white font-bold drop-shadow-lg"><%= bike.name %></h1>
            </div>
            <div class="space-y-4">
                <p class="text-gray-600 leading-relaxed"><%= bike.description %></p>
                <p class="text-xl text-gray-800 font-bold">Price: ₹<%= bike.price %></p>
                <a href="/payment?bikeId=<%= bike._id %>" class="block bg-gradient-to-r from-blue-500 to-indigo-500 text-white text-center py-3 rounded-lg font-semibold shadow-lg hover:from-blue-600 hover:to-indigo-600 transition-all duration-200 transform hover:-translate-y-1 hover:shadow-xl">
                    Proceed to Pay
                </a>
            </div>
        </div>

        <!-- Related Bikes Section -->
        <div class="mt-10">
            <h2 class="text-2xl font-semibold text-gray-800 mb-6">Related Bikes</h2>
            <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-2 lg:grid-cols-4 gap-6">
                <% relatedBikes.forEach(relatedBike => { %>
                    <div class="bg-white p-4 rounded-lg shadow-lg hover:shadow-xl transition-shadow duration-300">
                        <img src="<%= relatedBike.image %>" alt="<%= relatedBike.name %>" class="w-full h-40 object-cover rounded-t-lg mb-4">
                        <h3 class="text-lg font-semibold mb-2 text-gray-800"><%= relatedBike.name %></h3>
                        <p class="text-gray-700 mb-2">Price: ₹<%= relatedBike.price %></p>
                        <a href="/buy-now/<%= relatedBike._id %>" class="block bg-blue-500 text-white text-center py-2 rounded-lg hover:bg-blue-600 transition duration-200">
                            View Details
                        </a>
                    </div>
                <% }) %>
            </div>
        </div>
    </div>
</body>

</html>
